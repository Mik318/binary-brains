import {ModuleContext} from "../shared/models/routing/module-context";

export class AppModules {
  static readonly home: ModuleContext = {
    id: 'home',
    description: 'Pantalla de inicio',
  }
  static readonly inicio: ModuleContext = {
    id: 'inicio',
    description: 'Pantalla de inicio',
  }
}
